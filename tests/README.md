# Tests

Automated security regression checks run in GitHub Actions. They protect the key
workspace isolation, identity, audit, upload, and browser-header safeguards from
being accidentally removed. Firebase Emulator rule tests and offline-sync tests
will be added when the related Firebase services are enabled for this project.
